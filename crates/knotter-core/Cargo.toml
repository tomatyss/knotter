[package]
name = "knotter-core"
version = "0.6.0"
edition = "2021"
license = "Apache-2.0"

[dependencies]
chrono = "0.4"
serde = { version = "1", features = ["derive"] }
thiserror = "2"
uuid = { version = "1", features = ["v4", "serde"] }
