[package]
name = "knotter-store"
version = "0.2.1"
edition = "2021"
license = "Apache-2.0"

[dependencies]
knotter-core = { path = "../knotter-core" }
dirs = "6"
chrono = "0.4"
rusqlite = { version = "0.38", features = ["bundled", "backup"] }
thiserror = "2"

[dev-dependencies]
tempfile = "3"
